server {
  host=${?POOL_BALANCE_Z_HOST}
  port=${?POOL_BALANCE_Z_PORT}
  endpoint=${?POOL_BALANCE_Z_ENDPOINT}
}

ds {
  dataSourceClassName=${?POOL_BALANCE_Z_POSTGRESQL_DRIVER}
  dataSource.user=${?POOL_BALANCE_Z_POSTGRESQL_USER}
  dataSource.password=${?POOL_BALANCE_Z_POSTGRESQL_PASSWORD}
  dataSource.databaseName=${?POOL_BALANCE_Z_POSTGRESQL_DB_NAME}
  dataSource.serverName=${?POOL_BALANCE_Z_POSTGRESQL_HOST}
  dataSource.portNumber=${?POOL_BALANCE_Z_POSTGRESQL_PORT}
}

email {
  host=${?POOL_BALANCE_Z_EMAIL_HOST}
  sender=${?POOL_BALANCE_Z_EMAIL_ADDRESS}
  password=${?POOL_BALANCE_Z_EMAIL_PASSWORD}
}